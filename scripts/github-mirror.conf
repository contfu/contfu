# Paths to sync from Forgejo to the GitHub mirror.
# Directories are synced recursively. Files are synced as-is.
# Lines starting with # are comments. Empty lines are ignored.

# Packages (synced as-is)
packages/core
packages/cli
packages/client/client
packages/client/connect
packages/client/bun-file-store
packages/client/media-optimizer
packages/client/media-optimizer-remote
packages/service/core

# Docs (docs/private/ is excluded via rsync --exclude)
docs

# Docker
Dockerfile.client

# Shared root configs
tsconfig.json
.oxlintrc.json
.oxfmtrc.json
.editorconfig
bunfig.toml
.bun-version
.gitignore
.dockerignore
