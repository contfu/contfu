{
  "name": "@contfu/db",
  "main": "src/index.ts",
  "type": "module",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "build": "bun build --minify --sourcemap --target=bun src/migrator.ts --outfile dist/migrator.js",
    "dev": "bun src/migrator.ts"
  },
  "devDependencies": {
    "@types/bun": "*",
    "drizzle-kit": "^0.27.2"
  },
  "peerDependencies": {
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "drizzle-orm": "^0.36.1"
  },
  "files": [
    "dist"
  ]
}
