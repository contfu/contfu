{
  "name": "@contfu/client",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build:types": "tsc",
    "build:bundle": "bun build ./src/index.ts --outdir dist --target node --sourcemap=external --minify --external kysely"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "kysely-bun-sqlite": "^0.3.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "kysely": "^0.27.3"
  }
}
