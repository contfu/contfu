# Paths to sync from Forgejo to the GitHub mirror.
# Directories are synced recursively. Files are synced as-is.
# Lines starting with # are comments. Empty lines are ignored.

# Packages (synced as-is)
packages/core
packages/cli
packages/connect
packages/contfu
packages/client
packages/server
packages/ui
packages/media/bun-file-store
packages/media/media-optimizer
packages/media/media-optimizer-remote
packages/service/core
packages/service/api

# Docker
docker/Dockerfile.ui
# docker/Dockerfile.server — maintained separately on GitHub (no service packages)

# Scripts
scripts/pack-package.ts
scripts/retry-timeout.sh

# Docs (docs/private/ is excluded via rsync --exclude)
docs

# Shared root configs
.oxlintrc.json
.oxfmtrc.json
.editorconfig
bunfig.toml
.bun-version
.gitignore
.dockerignore
