{
  "name": "contfu",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun --filter '@contfu/*' test",
    "build": "bun --filter '@contfu/*' build"
  },
  "devDependencies": {
    "@types/bun": "^1.1.10"
  },
  "peerDependencies": {
    "typescript": "^5.6.2"
  },
  "workspaces": [
    "client/*",
    "server/*",
    "core"
  ]
}
