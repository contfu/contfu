{
  "name": "contfu",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun --filter '@contfu/*' test",
    "build": "bun --filter '@contfu/*' build",
    "dev": "bun --filter '@contfu/*' dev"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5"
  },
  "peerDependencies": {
    "typescript": "^5.6.3"
  },
  "workspaces": [
    "packages/client/*",
    "services/*",
    "packages/core",
    "packages/sync/*"
  ]
}
