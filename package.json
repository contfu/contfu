{
  "name": "contfu",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun --filter '@contfu/*' test",
    "build": "bun --filter '@contfu/*' build",
    "dev": "bun --filter '@contfu/*' dev"
  },
  "devDependencies": {
    "@types/bun": "*"
  },
  "peerDependencies": {
    "typescript": "^5.6.3"
  },
  "workspaces": [
    "packages/client/*",
    "packages/server/*",
    "packages/core",
    "packages/sync/*"
  ]
}
